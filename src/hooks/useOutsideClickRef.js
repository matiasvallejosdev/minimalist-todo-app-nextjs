'use client'
import { useRef, useEffect } from "react";

const useOutsideClickRef = (callback) => {
    /* Execute callback when user click outside the reference element */


    return { backgroundRef }
}

export default useOutsideClickRef;